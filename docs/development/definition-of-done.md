# 完成定义

- [ ] 需求目标、非目标、活动状态和验收条件明确且已满足。
- [ ] 模块依赖保持 `startup → biz → api`，Controller、Service、DAO、DTO、VO 与 PO 未越层。
- [ ] 新增同步写契约已登记调用方、服务身份、权限、事务、幂等、重试、兼容、补偿和回滚。
- [ ] 主体、请求和数据记录的 `organId` 一致，`WithoutIsolation` 路径具有显式保护。
- [ ] 密码、Token、IdP 凭证和个人信息按最小必要原则处理，日志、配置、响应和文档无真实秘密。
- [ ] 逻辑删除唯一键语义明确，迁移和历史数据已评估。
- [ ] 必要的单元及集成测试已补充，`mvn clean verify` 通过。
- [ ] API、数据库、配置、部署、架构、需求和 README 已同步。
- [ ] Markdown 链接、项目元数据、POM、启动类、端口和镜像声明一致。
- [ ] Git Diff 不包含生成器误覆盖、调试代码、构建产物、日志和无关修改。
